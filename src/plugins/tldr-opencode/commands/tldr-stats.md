---
description: Show TLDR lifetime token-savings stats
---
Show TLDR stats — total tokens saved, sessions, average compression ratio.

Read the lifetime history log at `~/.config/tldr/.tldr-history.jsonl`
(or wherever the tldr-stats script writes it). Output: total saved,
sessions counted, avg ratio. One short table.
